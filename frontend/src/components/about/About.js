import React from "react";
import { githubURL } from "../constants";

export function About(props) {
  return (
    <div className={"about-text"}>
      <p>
        Just a Django+React project
      </p>
    </div>
  );
}
